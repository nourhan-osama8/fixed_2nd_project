# LLM constants – sourced from Settings so nothing is hard-coded here.
# Import these in other modules instead of repeating magic strings.

TEMPERATURE: float = 0.1
MAX_COMPLETION_TOKENS: int = 1200
MAX_AGENT_STEPS: int = 6
